apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: voting-app
  name: db-pvc

spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 1Gi
